<nav class="site-nav">
    <div class="site-nav-left">
        {{^is "home"}}
            {{#if @blog.logo}}
                <a class="site-nav-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>
            {{else}}
                <a class="site-nav-logo" href="{{@blog.url}}">{{@blog.title}}</a>
            {{/if}}
        {{/is}}
        {{#if @blog.navigation}}
            {{navigation}}
        {{/if}}
    </div>
    <div class="site-nav-right">
        <div class="social-links">
            {{#if @blog.facebook}}
                <a class="social-link" href="{{facebook_url @blog.facebook}}" target="_blank"><i class="fa fa-facebook fa-2x" aria-hidden="true"></i></a>
            {{/if}}
            {{#if @blog.twitter}}
                <a class="social-link" href="{{twitter_url @blog.twitter}}" target="_blank"><i class="fa fa-twitter fa-2x" aria-hidden="true"></i></a>
            {{/if}}
            <a class="social-link" href="https://github.com/makerdiary/" target="_blank"><i class="fa fa-github fa-2x" aria-hidden="true"></i></a>
        </div>
        {{#if @labs.subscribers}}
            <a class="subscribe-button" href="#subscribe">Subscribe</a>
        {{else}}
            <a class="rss-button" href="http://cloud.feedly.com/#subscription/feed/{{@blog.url}}/rss/" target="_blank">{{> "icons/rss"}}</a>
        {{/if}}
    </div>
</nav>
